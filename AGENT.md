# VERSION

Assistant protocol document version: 0.5.0

The version file the UI loads at runtime is `letter_writer_web/public/app-version.txt` (copied to the site root as `/app-version.txt`). The `letter_writer_web/dist/` copy is build output only and should match after `npm run build`.
