# Agent metadata

**Application version (label next to the title in the web UI)** is read at runtime from:

`letter_writer_web/public/app-version.txt`

(one line, `major.minor.patch`; loader: `letter_writer_web/src/appVersion.js`)

Agent session metadata (not shown in the UI):

version: 0.3.0
